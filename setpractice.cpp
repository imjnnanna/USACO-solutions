/*
#include <iostream>
#include <set>
using namespace std;
int main() {
    int n;
    string a;
    cin >> n;
    set<string> names;
    for(int i = 0; i < n; i++) {
        cin >> a;
        names.insert(a);
    }
    for(auto x:names) {
        cout << x << ' ';
    }
    cout << endl;
}
*/
