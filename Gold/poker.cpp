/*
#include <iostream>
using namespace std;
int main() {
    long long n;
    cin >> n;
    long long prev = 0, ans = 0, a;
    for(int i = 0; i < n; i++) {
        cin >> a;
        if(a - prev > 0) ans += a - prev;
        prev = a;
    }
    cout << ans << endl;
}
*/
