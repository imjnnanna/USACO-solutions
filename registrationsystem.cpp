/*
#include <iostream>
#include <map>
using namespace std;
map<string, int> names;
int main() {
    int n;
    string a;
    cin >> n;
    for(int i = 0; i < n; i++) {
        cin >> a;
        if(names.count(a) == 0) cout << "OK" << endl;
        else cout << a << names[a] - 1 << endl;
        names[a]++;
    }
}
*/
